define('admin/plugins/sso-identity', ['settings'], function(Settings) {
	'use strict';
	/* globals $, app, socket, require */

	var ACP = {};

	ACP.init = function() {
		Settings.load('sso-identity', $('.sso-identity-settings'));

		$('#save').on('click', function() {
			Settings.save('sso-identity', $('.sso-identity-settings'), function() {
				app.alert({
					type: 'success',
					alert_id: 'sso-identity-saved',
					title: 'Settings Saved',
					message: 'Please reload your NodeBB to apply these settings',
					clickfn: function() {
						socket.emit('admin.reload');
					}
				});
			});
		});
	};

	return ACP;
});